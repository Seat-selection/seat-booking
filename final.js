const returnBtn = document.getElementById('finished-button');
    if (returnBtn) {
        returnBtn.addEventListener('click', function(){
            
            window.location.href = 'seat-selection-page.html';
        })
    }